# --- Slack (get from api.slack.com/apps > Your App > Basic Information) ---
SLACK_BOT_TOKEN=xoxb-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
SLACK_APP_TOKEN=xapp-xxxxxxxxxxxxxxxxxxxxxxxxxxxx     # required for Socket Mode
SLACK_SIGNING_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Delivery mode ---
# SOCKET (dev) or HTTP (prod-style)
DELIVERY_MODE=SOCKET
PUBLIC_BASE_URL=https://your.domain.com   # only used if DELIVERY_MODE=HTTP
PORT=3109

# --- ChatBot (Option B default) ---
CHATBOT_API_URL=http://localhost:3003/chat
CHATBOT_API_KEY=replace-with-chatbot-service-key

# --- Integration mode (Option A later) ---
MODE=optionB

# --- Local storage (encrypted user link store) ---
ENCRYPTION_KEY=REPLACE_WITH_32_BYTE_BASE64  # run: npm run keygen
DATABASE_PATH=./data/data.db

# --- Secure the /link/callback endpoint ---
LINK_SHARED_SECRET=dev-link-secret

# --- Logging ---
NODE_ENV=development
LOG_LEVEL=debug

# --- Linking requirement ---
# Set to 'true' to require /link before /ask. Set to 'false' to let ChatBot auth by Slack IDs.
REQUIRE_LINKING=false
